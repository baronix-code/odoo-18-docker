[options]
; Puerto donde corre Odoo
http_port = 8069

; Base de datos por defecto
db_name = False

; Datos para conexión con PostgreSQL externo
db_host = db
db_port = 5432
db_user = odoo
db_password = odoo

; Rutas para módulos: personalizados, core modificables y core estándar
addons_path = /mnt/extra-addons,/mnt/core-addons,/usr/lib/python3/dist-packages/odoo/addons

; Directorio para datos internos de Odoo (persistencia dentro del contenedor)
data_dir = /var/lib/odoo

; Archivo de logs
logfile = /var/log/odoo/odoo.log

; Nivel de logs
log_level = info

; Modo desarrollador activado (cambiar a False en producción)
developer_mode = True
